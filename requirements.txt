# Core scientific stack (versions pinned for reproducibility)
numpy>=1.26,<3.0
pandas>=2.1,<3.0
scipy>=1.11,<2.0
scikit-learn>=1.3,<2.0
matplotlib>=3.7,<4.0
PyYAML>=6.0,<7.0

# Optional meta-labelling (thin layer, OFF by default). Uncomment to enable.
# lightgbm>=4.0,<5.0

# Tests
pytest>=7.0

# NOTE on MetaTrader5:
# The MT5 python package is imported lazily inside MT5Source only when a live
# connection is requested. It is NOT required to run tests or the synthetic
# pipeline. Install it separately on a Windows box with a live MT5 terminal:
#   pip install MetaTrader5
